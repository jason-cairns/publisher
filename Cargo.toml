[package]
name = "publisher"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
typst = "0.14.2"
typst-html = "0.14.2"
typst-kit = { version = "0.14.2", default-features = false, features = [
    "embed-fonts",
] }
typst-pdf = "0.14.2"
typst-syntax = "0.14.2"

[dev-dependencies]
insta = "1.43.1"
